# Sanity CMS Configuration
# Copy this file to .env.local and fill in your values
# Get these values from https://www.sanity.io/manage

# Your Sanity Project ID (found in your Sanity project settings)
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id_here

# Your Sanity Dataset (usually 'production' or 'development')
NEXT_PUBLIC_SANITY_DATASET=production

# Read-only token for accessing Sanity data (REQUIRED for document IDs with dots)
# Get from: https://sanity.io/manage → API → Tokens → Add token → Viewer permissions
# This is safe to expose in the browser (read-only access)
NEXT_PUBLIC_SANITY_READ_TOKEN=your_read_only_token_here

# Write token (only needed for seeding/updating content)
SANITY_API_TOKEN=your_api_token_here
SANITY_REVALIDATE_SECRET=your_revalidate_secret_here


